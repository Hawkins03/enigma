#ifdef TERM_H

#include "init.h"
#include "encrypt.h"

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define NULL_PTR        (-1)

int find_str_len(char **, int, char *, int);
char *join_str(char **, int, char *, int);

#endif /* TERM_H */
